#include <stdio.h>

int main()
{
	char unnume='O';			/* The letter 'o' */
	printf("This dictionary carries words from letter %c ", unnume);
	unnume = 'Z';
	printf("to letter %c.", unnume);

	return (0);
}