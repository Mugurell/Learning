#include <stdio.h> 
#include <math.h> 

int main()
{
	double cube;
	double putere;
	cube = cbrt(2.0);
	putere = pow(2,0.33333);

	printf(" The cube root of 2 is %f\n", cube);
	printf(" 2 to the 1/3 power is %f" , putere);
	return (0);
}