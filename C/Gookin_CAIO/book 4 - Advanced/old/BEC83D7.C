#include <stdio.h>

int main()
{
     unsigned int x = 0xBEC83D7;

    printf("The decimal value of 0xBEC83D7 is %d\r." , x);
    getchar();
 /*
    printf("Enter here the value you want transformed in hexa %c ", 16);
    scanf("%d", &x);
    while(getchar() != '\n')
        continue;

    printf("%i in hexa means 0x%x", x, x);
    getchar();
*/

    return (6365);
}
